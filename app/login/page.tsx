"use client";
import { FormEvent, useState } from "react";
import { createClient } from "../../lib/supabase/client";
import "../polish.css";
export default function LoginPage() { const supabase = createClient(); const [mode, setMode] = useState<"login" | "signup">("login"); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [name, setName] = useState(""); const [company, setCompany] = useState(""); const [message, setMessage] = useState(""); const [loading, setLoading] = useState(false);
async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setLoading(true); setMessage(""); const result = mode === "login" ? await supabase.auth.signInWithPassword({ email, password }) : await supabase.auth.signUp({ email, password, options: { data: { full_name: name, company_name: company } } }); setLoading(false); if (result.error) { setMessage(result.error.message); return; } if (mode === "signup") { setMessage("Conta criada. Verifique seu e-mail para confirmar o acesso."); return; } window.location.href = "/"; }
return <main className="auth-shell"><section className="auth-brand"><div className="brand large"><span className="brand-mark">G</span><span>Gestão<span className="muted"> Vendas Tech</span></span></div><div className="auth-copy"><span className="eyebrow">GESTÃO COMERCIAL EXTERNA</span><h1>Venda melhor.<br/><em>Visite com propósito.</em></h1><p>Clientes, agenda, rotas e oportunidades em um só lugar.</p></div><div className="auth-foot">Plataforma comercial para equipes que estão em campo.</div></section><section className="auth-panel"><div className="auth-card"><span className="eyebrow">{mode === "login" ? "BEM-VINDO DE VOLTA" : "COMECE AGORA"}</span><h2>{mode === "login" ? "Acesse sua operação" : "Crie sua empresa"}</h2><p className="auth-muted">{mode === "login" ? "Entre para acompanhar sua equipe comercial." : "Configure seu espaço comercial em poucos minutos."}</p><form onSubmit={submit}>{mode === "signup" && <><label>Seu nome<input required value={name} onChange={e => setName(e.target.value)} placeholder="Como devemos chamar você?" /></label><label>Nome da empresa<input required value={company} onChange={e => setCompany(e.target.value)} placeholder="Ex.: Grupo Horizonte" /></label></>}<label>E-mail<input required type="email" value={email} onChange={e => setEmail(e.target.value)} placeholder="voce@empresa.com" /></label><label>Senha<input required minLength={6} type="password" value={password} onChange={e => setPassword(e.target.value)} placeholder="Mínimo de 6 caracteres" /></label>{message && <p className="auth-message">{message}</p>}<button className="primary auth-submit" disabled={loading}>{loading ? "Aguarde..." : mode === "login" ? "Entrar na plataforma" : "Criar minha conta"}</button></form><button className="switch-auth" onClick={() => { setMode(mode === "login" ? "signup" : "login"); setMessage(""); }}>{mode === "login" ? "Ainda não tenho uma conta" : "Já tenho uma conta"}</button></div></section></main>; }
